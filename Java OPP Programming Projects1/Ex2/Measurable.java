/**
 * This interface states that all implementations of type measurable
 * must contain a getValue method that takes no input arguments and returns
 * a double value.
 */

public interface Measurable {

    public double getValue();
}
